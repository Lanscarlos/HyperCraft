# Java 运行环境支持 Azul Zulu 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `internal/javaruntime` 里写死的 Adoptium 抽成一根发行版轴，让面板能装 Azul Zulu 并默认用它，下载源随发行版分表，顺带做通 Alpine（musl）。

**Architecture:** 包内新增私有 `provider` 接口，`adoptiumProvider` 和 `azulProvider` 各实现一份，`Client` 退化成按发行版分发的门面。`Release` 带上 `Distribution` 字段，于是 `Fetch`、`installID`、错误消息都能从 release 自己知道该问哪张源表，不必一路加形参。下载源拆成 `temurinSources` / `zuluSources` 两张表。

**Tech Stack:** Go 1.x（标准库，无新依赖）、React 18 + TypeScript + Vite。

**Spec:** `docs/proposal-java-zulu.md`

## Global Constraints

- **代码注释用英文，文档和用户可见文案用中文。** 沿用所处文件的语言，不要混。
- 注释解释**为什么**，不解释代码在做什么。
- Go 提交前必须 `gofmt`（CI 直接卡）。
- 全部样式在 `web/src/styles.css`，**本计划不新增任何 CSS**——复用现成的 `choice-grid` / `choice` / `segmented` 类。
- 用户可见的行为变化写进 `CHANGELOG.md` 的「未发布」小节，**不要**改成版本号标题。
- 发行版 id 同时是安装目录名前缀：`zulu` / `temurin`。
- Azul 查询必须显式带 `javafx_bundled=false`、`crac_supported=false`、`release_status=ga`、`availability_type=ca`，否则会拿到 CRaC 或捆 JavaFX 的包。
- **Azul 响应里的 `arch` 字段不可信**（查 x64 返回 `"arch":"x86"`，查 aarch64 返回 `"arch":"arm"`）。`Release.Arch` 一律填请求时的 `platform.Arch`。
- 默认发行版是 `DistZulu`。
- 后端检查：`make lint && make test`。前端检查：`npm --prefix web run build`。

### 与方案文档的三处偏差（已确认，按本计划为准）

1. 方案写的 majors 端点是 `/api/java/majors`，**真实路由是 `/api/java/available`**（`api.ts:594`）。
2. 方案说 `Majors` 缓存要变成 `map[string][]Major` 挂在 `Client` 上；实际做法是**每个 provider 自己持有缓存**，`Client` 不再有缓存字段——更简单，且天然不会串。
3. 方案未提 `archive_type`。Azul 必须显式给：Windows 用 `zip`，其余用 `tar.gz`。`os` / `arch` 的取值两边通用（实测 Azul 对 `mac` 和 `aarch64` 都认），**不需要**第二张映射表。

---

## 文件结构

| 文件 | 责任 |
| --- | --- |
| `internal/javaruntime/fetch.go` | **新**。`httpClient`：带 UA 的 HTTP、`getJSON`、`open`、`checkDownloadURL`；以及 `safeFileName`、`isSupportedArchive`、`majorCache`。两个 provider 共用。 |
| `internal/javaruntime/distribution.go` | **新**。`DistZulu` / `DistTemurin`、`Distribution` 类型、`Distributions()`、`ResolveDistribution`、`DistributionName`、`provider` 接口。 |
| `internal/javaruntime/adoptium.go` | 收缩成 `adoptiumProvider`，只留 Adoptium 独有的查询与解析。 |
| `internal/javaruntime/azul.go` | **新**。`azulProvider`。 |
| `internal/javaruntime/client.go` | **新**。`Client` 门面：持有两个 provider，按 id 分发。 |
| `internal/javaruntime/source.go` | 两张源表、自定义前缀、`attempts(dist, id, release)`。 |
| `internal/javaruntime/platform.go` | `Platform.LibC`、`detectLibC()`、`PlatformWarning(dist, platform)`。 |
| `internal/javaruntime/store.go` | `inspect` 增加 `bin/javac` 判 image type 的回落。 |
| `internal/javaruntime/installer.go` | `Start` 多一个 `dist` 形参，`installID` 用 `release.Distribution`。 |
| `internal/api/handlers_java.go` | 三个端点加发行版字段、`javaDistribution()` / `rememberJavaDistribution()`。 |
| `internal/config/config.go` | `Panel.JavaDistribution`。 |
| `web/src/types.ts` / `api.ts` / `useJava.ts` / `components/JavaPage.tsx` | 前端。 |

---

## Task 1: 抽出 fetch.go，adoptium.go 收缩成 provider

纯重构，**行为零变化**，现有测试必须原样全绿。单独成一个提交，因为它 diff 大而无害，混进后面几步会让真正有行为变化的改动淹掉。

**Files:**
- Create: `internal/javaruntime/fetch.go`
- Create: `internal/javaruntime/client.go`
- Modify: `internal/javaruntime/adoptium.go`
- Test: 不新增，跑现有 `internal/javaruntime` 全部测试

**Interfaces:**
- Produces: `httpClient`（`newHTTPClient(userAgent string) *httpClient`、`(*httpClient).getJSON(ctx, url string, dst any) error`、`(*httpClient).open(ctx, link string) (io.ReadCloser, error)`、`(*httpClient).checkDownloadURL(raw string) error`）；`majorCache`（`(*majorCache).get() ([]Major, bool)`、`(*majorCache).put([]Major)`）；`adoptiumProvider`。
- Produces: `NewClient(userAgent string, bases map[string]string) *Client` —— **签名变了**，Task 7 之前 `cmd/hypercraft/main.go` 和 `internal/api/server_test.go` 都要跟着改。

- [ ] **Step 1: 建 `fetch.go`，把共用部分搬过去**

搬 `Client` 上的 `http`、`userAgent`、`getJSON`、`checkDownloadURL`，以及包级的 `safeFileName`、`isSupportedArchive`。注释原样带走——它们记的是踩过的坑。

```go
package javaruntime

import (
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"path"
	"strings"
	"sync"
	"time"
)

// httpClient is the transport both distributions share: a metadata GET that
// returns a few kilobytes of JSON, and an archive GET that streams 50–200 MB.
type httpClient struct {
	userAgent string
	http      *http.Client
}

func newHTTPClient(userAgent string) *httpClient {
	if userAgent == "" {
		userAgent = "HyperCraft"
	}
	return &httpClient{
		userAgent: userAgent,
		http: &http.Client{
			// No overall timeout: this client also streams a 50 MB tarball.
			Transport: &http.Transport{
				Proxy:                 http.ProxyFromEnvironment,
				TLSHandshakeTimeout:   15 * time.Second,
				ResponseHeaderTimeout: 30 * time.Second,
				IdleConnTimeout:       60 * time.Second,
			},
		},
	}
}

func (c *httpClient) getJSON(ctx context.Context, endpoint string, dst any) error {
	req, err := http.NewRequestWithContext(ctx, http.MethodGet, endpoint, nil)
	if err != nil {
		return err
	}
	req.Header.Set("User-Agent", c.userAgent)
	req.Header.Set("Accept", "application/json")

	resp, err := c.http.Do(req)
	if err != nil {
		return fmt.Errorf("%w: %v", ErrUpstream, err)
	}
	defer resp.Body.Close()

	switch {
	case resp.StatusCode == http.StatusNotFound:
		return fmt.Errorf("%w: %s", ErrUnknownRelease, endpoint)
	case resp.StatusCode == http.StatusTooManyRequests:
		return fmt.Errorf("%w: rate limited, try again in a minute", ErrUpstream)
	case resp.StatusCode != http.StatusOK:
		return fmt.Errorf("%w: HTTP %d from %s", ErrUpstream, resp.StatusCode, endpoint)
	}

	if err := json.NewDecoder(io.LimitReader(resp.Body, 8<<20)).Decode(dst); err != nil {
		return fmt.Errorf("%w: malformed response from %s: %v", ErrUpstream, endpoint, err)
	}
	return nil
}

func (c *httpClient) open(ctx context.Context, link string) (io.ReadCloser, error) {
	if err := c.checkDownloadURL(link); err != nil {
		return nil, err
	}
	req, err := http.NewRequestWithContext(ctx, http.MethodGet, link, nil)
	if err != nil {
		return nil, err
	}
	req.Header.Set("User-Agent", c.userAgent)

	resp, err := c.http.Do(req)
	if err != nil {
		return nil, fmt.Errorf("%w: %v", ErrUpstream, err)
	}
	if resp.StatusCode != http.StatusOK {
		resp.Body.Close()
		return nil, fmt.Errorf("%w: download returned HTTP %d", ErrUpstream, resp.StatusCode)
	}
	return resp.Body, nil
}

// allowPlainHTTP is set by the tests, whose fake metadata APIs are served over
// plain HTTP and hand out plain-HTTP download links.
var allowPlainHTTP bool

// checkDownloadURL keeps an upstream-supplied URL to plain HTTPS. Both
// distributions serve assets from a CDN whose host varies and is not worth
// pinning, but the scheme is.
func (c *httpClient) checkDownloadURL(raw string) error {
	parsed, err := url.Parse(raw)
	if err != nil || parsed.Host == "" {
		return fmt.Errorf("%w: unusable download URL %q", ErrUpstream, raw)
	}
	switch parsed.Scheme {
	case "https":
		return nil
	case "http":
		if allowPlainHTTP {
			return nil
		}
	}
	return fmt.Errorf("%w: refusing to download over %q", ErrUpstream, parsed.Scheme)
}

// majorCache holds a distribution's feature-release list. The list changes a
// few times a year, so an hour is plenty and keeps the page snappy.
type majorCache struct {
	mu      sync.Mutex
	majors  []Major
	expires time.Time
}

const majorCacheTTL = time.Hour

func (c *majorCache) get() ([]Major, bool) {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.majors != nil && time.Now().Before(c.expires) {
		return c.majors, true
	}
	return nil, false
}

func (c *majorCache) put(majors []Major) {
	c.mu.Lock()
	defer c.mu.Unlock()
	c.majors, c.expires = majors, time.Now().Add(majorCacheTTL)
}

func safeFileName(name string) (string, error) {
	name = strings.TrimSpace(strings.ReplaceAll(name, "\\", "/"))
	base := path.Base(name)
	if name == "" || name != base || base == "." || base == ".." || strings.ContainsAny(base, "/\x00") {
		return "", fmt.Errorf("%w: unusable file name %q", ErrUpstream, name)
	}
	return base, nil
}

func isSupportedArchive(name string) bool {
	lower := strings.ToLower(name)
	return strings.HasSuffix(lower, ".tar.gz") || strings.HasSuffix(lower, ".zip")
}
```

> **注意 `allowPlainHTTP`。** 原来的 `checkDownloadURL` 靠 `strings.HasPrefix(c.baseURL, "http://")` 判断是否在测试里；`httpClient` 不再持有 baseURL，所以换成一个包级开关。Step 3 会在测试里打开它。

- [ ] **Step 2: `adoptium.go` 收缩成 `adoptiumProvider`**

删掉 `Client` 结构体和它的方法，改成：

```go
// DefaultBaseURL is the Adoptium API. Temurin is the reference OpenJDK build:
// no account, no click-through licence, and every asset comes with a checksum.
const DefaultBaseURL = "https://api.adoptium.net/v3"

// adoptiumProvider serves Eclipse Temurin.
type adoptiumProvider struct {
	baseURL string
	http    *httpClient
	cache   majorCache
}

func newAdoptiumProvider(baseURL string, client *httpClient) *adoptiumProvider {
	if baseURL == "" {
		baseURL = DefaultBaseURL
	}
	return &adoptiumProvider{baseURL: strings.TrimSuffix(baseURL, "/"), http: client}
}

func (p *adoptiumProvider) Majors(ctx context.Context) ([]Major, error) {
	if cached, ok := p.cache.get(); ok {
		return cached, nil
	}

	var payload struct {
		AvailableReleases    []int `json:"available_releases"`
		AvailableLTSReleases []int `json:"available_lts_releases"`
	}
	if err := p.http.getJSON(ctx, p.baseURL+"/info/available_releases", &payload); err != nil {
		return nil, err
	}
	if len(payload.AvailableReleases) == 0 {
		return nil, fmt.Errorf("%w: no releases listed", ErrUpstream)
	}

	lts := make(map[int]bool, len(payload.AvailableLTSReleases))
	for _, major := range payload.AvailableLTSReleases {
		lts[major] = true
	}
	majors := make([]Major, 0, len(payload.AvailableReleases))
	for _, major := range payload.AvailableReleases {
		majors = append(majors, Major{Major: major, LTS: lts[major]})
	}
	sort.Slice(majors, func(a, b int) bool { return majors[a].Major > majors[b].Major })

	p.cache.put(majors)
	return majors, nil
}
```

`LatestRelease` 原样搬过来，只有三处改动：接收者换成 `p *adoptiumProvider`；`c.getJSON(ctx, endpoint, ...)` 换成 `p.http.getJSON(ctx, p.baseURL+endpoint, ...)`；`c.checkDownloadURL` 换成 `p.http.checkDownloadURL`。返回的 `Release` 里**加一行** `Distribution: DistTemurin`（常量在 Task 2 建，本任务先不填，Task 2 补）。

- [ ] **Step 3: 建 `client.go`**

```go
package javaruntime

import (
	"context"
	"fmt"
	"io"
)

// Client is the panel's way in to every distribution it can install from.
//
// It holds one provider per distribution and does nothing else: the metadata
// shapes are wildly different between them, so the only thing worth sharing at
// this level is "which one are we talking to".
type Client struct {
	providers map[string]provider
	http      *httpClient
}

// NewClient builds a client for every distribution.
//
// bases overrides a distribution's metadata endpoint by id and exists for the
// tests, which serve fake APIs over httptest; nil means the real ones.
func NewClient(userAgent string, bases map[string]string) *Client {
	shared := newHTTPClient(userAgent)
	return &Client{
		http: shared,
		providers: map[string]provider{
			DistZulu:    newAzulProvider(bases[DistZulu], shared),
			DistTemurin: newAdoptiumProvider(bases[DistTemurin], shared),
		},
	}
}

func (c *Client) provider(dist string) (provider, error) {
	dist, err := ResolveDistribution(dist)
	if err != nil {
		return nil, err
	}
	return c.providers[dist], nil
}

// Majors lists the feature releases a distribution currently ships, newest
// first, with the long-term-support ones flagged.
func (c *Client) Majors(ctx context.Context, dist string) ([]Major, error) {
	p, err := c.provider(dist)
	if err != nil {
		return nil, err
	}
	return p.Majors(ctx)
}

// LatestRelease resolves the newest build of a major version for a platform.
func (c *Client) LatestRelease(ctx context.Context, dist string, major int, imageType string, platform Platform) (Release, error) {
	p, err := c.provider(dist)
	if err != nil {
		return Release{}, err
	}
	if major <= 0 || major > 999 {
		return Release{}, fmt.Errorf("%w: %d is not a Java version", ErrUnknownRelease, major)
	}
	if !validImageType(imageType) {
		return Release{}, fmt.Errorf("%w: image type must be jre or jdk", ErrUnknownRelease)
	}
	return p.LatestRelease(ctx, major, imageType, platform)
}

// Fetch opens the archive body from the requested download source, falling
// back through the rest as described in attempts. It returns the body — which
// the caller closes — and the id of the source that answered.
//
// Which sources exist is the release's own business: it carries the
// distribution it came from, so nothing upstream has to pass it along.
func (c *Client) Fetch(ctx context.Context, release Release, sourceID string) (io.ReadCloser, string, error) {
	tries := attempts(release.Distribution, sourceID, release)
	if len(tries) == 0 {
		return nil, "", fmt.Errorf("%w: unusable download URL %q", ErrUpstream, release.URL)
	}

	var lastErr error
	for _, try := range tries {
		if err := ctx.Err(); err != nil {
			return nil, "", err
		}
		body, err := c.http.open(ctx, try.url)
		if err == nil {
			return body, try.id, nil
		}
		lastErr = fmt.Errorf("%s: %w", SourceName(release.Distribution, try.id), err)
	}
	return nil, "", lastErr
}
```

> `LatestRelease` 里那两条 major / imageType 的校验从 provider 提到了门面：两个 provider 的规则一样，重复两遍只会漂移。

- [ ] **Step 4: 改三个调用点**

`cmd/hypercraft/main.go:151`：

```go
javaruntime.NewClient(userAgent, nil),
```

`internal/api/server_test.go:113`：

```go
javaruntime.NewClient("test", map[string]string{javaruntime.DistTemurin: adoptium.URL()}),
```

`internal/javaruntime/adoptium_test.go` 里所有 `NewClient(upstream.URL, "test")` 换成：

```go
newTestClient(map[string]string{DistTemurin: upstream.URL})
```

并在 `fixtures_test.go` 末尾加这个助手（它同时打开明文 HTTP 开关）：

```go
// newTestClient points a client at fake metadata APIs and lets it follow the
// plain-HTTP download links they hand out.
func newTestClient(bases map[string]string) *Client {
	allowPlainHTTP = true
	return NewClient("test", bases)
}
```

对应地，`client.Majors(context.Background())` 全部补上发行版实参：`client.Majors(context.Background(), DistTemurin)`，`LatestRelease` 同理补 `DistTemurin`。

- [ ] **Step 5: 跑测试**

Run: `make lint && go test ./internal/javaruntime/... ./internal/api/... 2>&1 | tail -20`
Expected: 全绿。**这一步没有新行为，任何红都是搬运时漏了东西。**

- [ ] **Step 6: 提交**

```bash
git add internal/javaruntime cmd/hypercraft/main.go internal/api/server_test.go
git commit -m "重构：javaruntime 抽出共用的 HTTP 层与 provider

Adoptium 的查询、缓存和 HTTP 搅在一个 Client 里，第二个发行版没有
地方可放。先把两边都要用的部分（带 UA 的 GET、JSON 解码、下载 URL
的 scheme 白名单、文件名消毒、大版本缓存）抽进 fetch.go，Adoptium
自己收缩成 adoptiumProvider，Client 退成按发行版分发的门面。

纯重构，行为零变化。"
```

---

## Task 2: 发行版这根轴

**Files:**
- Create: `internal/javaruntime/distribution.go`
- Create: `internal/javaruntime/distribution_test.go`
- Modify: `internal/javaruntime/adoptium.go`（`Release` 填 `Distribution`）
- Modify: `internal/javaruntime/adoptium.go` 的 `Release` 结构体定义处（加字段）
- Modify: `internal/javaruntime/installer.go`（`installID`、`Start`、`Job`）

**Interfaces:**
- Consumes: Task 1 的 `provider` 接口位置（本任务正式定义它）。
- Produces: `DistZulu` / `DistTemurin` 常量；`Distribution` 结构体；`Distributions() []Distribution`；`ResolveDistribution(id string) (string, error)`；`DistributionName(id string) string`；`ErrUnknownDistribution`；`Release.Distribution` 字段；`Installer.Start(dist string, major int, imageType, source string) (Job, error)`；`Job.Distribution` 字段。

- [ ] **Step 1: 写失败的测试**

`internal/javaruntime/distribution_test.go`：

```go
package javaruntime

import (
	"errors"
	"testing"
)

func TestDefaultDistributionIsZulu(t *testing.T) {
	resolved, err := ResolveDistribution("")
	if err != nil {
		t.Fatalf("ResolveDistribution(\"\"): %v", err)
	}
	if resolved != DistZulu {
		t.Errorf("an unset distribution resolved to %q, want %q", resolved, DistZulu)
	}

	list := Distributions()
	if len(list) != 2 {
		t.Fatalf("expected two distributions, got %+v", list)
	}
	if list[0].ID != DistZulu || !list[0].Default {
		t.Errorf("Zulu should be first and marked default, got %+v", list[0])
	}
}

// An unrecognised distribution is refused rather than quietly turned into the
// default: silently installing a different vendor's Java than what was asked
// for is exactly the surprise this axis exists to remove.
func TestResolveDistributionRefusesTheUnknown(t *testing.T) {
	if _, err := ResolveDistribution("graalvm"); !errors.Is(err, ErrUnknownDistribution) {
		t.Errorf("expected ErrUnknownDistribution, got %v", err)
	}
}
```

在 `installer_test.go` 里加：

```go
func TestInstallIDCarriesTheDistribution(t *testing.T) {
	zulu := installID(Release{Distribution: DistZulu, Version: "21.0.12.1", ImageType: ImageJRE})
	if zulu != "zulu-21.0.12.1-jre" {
		t.Errorf("zulu install id = %q", zulu)
	}
	// The + becomes a dash: installID keeps only alphanumerics and dots.
	temurin := installID(Release{Distribution: DistTemurin, Version: "21.0.12+8-LTS", ImageType: ImageJRE})
	if temurin != "temurin-21.0.12-8-jre" {
		t.Errorf("temurin install id = %q", temurin)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/javaruntime/ -run 'Distribution|InstallID' -v 2>&1 | tail -20`
Expected: 编译失败，`undefined: ResolveDistribution`、`undefined: DistZulu`。

- [ ] **Step 3: 写 `distribution.go`**

```go
package javaruntime

import (
	"context"
	"errors"
	"fmt"
)

// ErrUnknownDistribution is returned for an OpenJDK distribution we do not have.
var ErrUnknownDistribution = errors.New("unknown java distribution")

// Distribution ids. These double as the directory-name prefix of anything
// installed, so changing one orphans every runtime already on disk.
const (
	// DistZulu is Azul Zulu, the default: it builds for musl as well as glibc,
	// covers three feature releases Adoptium does not (13, 14, 15), and serves
	// its archives off a commercial CDN rather than GitHub's release storage.
	DistZulu = "zulu"
	// DistTemurin is Eclipse Temurin, kept because the mirrors that make a
	// download work from inside China all carry the Adoptium tree and none of
	// them carries Zulu.
	DistTemurin = "temurin"
)

// DefaultDistribution is what an install that names none gets.
const DefaultDistribution = DistZulu

// Distribution is an OpenJDK build the panel can install.
type Distribution struct {
	ID   string `json:"id"`
	Name string `json:"name"`
	Note string `json:"note"`
	// Default marks the one an install that names none gets.
	Default bool `json:"default,omitempty"`
}

// provider is one distribution's upstream: what it ships, and which build of
// it fits this machine.
type provider interface {
	Majors(ctx context.Context) ([]Major, error)
	LatestRelease(ctx context.Context, major int, imageType string, platform Platform) (Release, error)
}

// distributions are offered to the operator in this order, default first.
var distributions = []Distribution{
	{
		ID:      DistZulu,
		Name:    "Azul Zulu",
		Note:    "官方 CDN 直连，国内一般比 GitHub 好走；musl（Alpine）也能装",
		Default: true,
	},
	{
		ID:   DistTemurin,
		Name: "Eclipse Temurin",
		Note: "有清华、南大、华为等国内镜像；不支持 musl",
	},
}

// Distributions lists what an operator can pick, default first.
func Distributions() []Distribution {
	out := make([]Distribution, len(distributions))
	copy(out, distributions)
	return out
}

// DistributionName is the human name of a distribution id, for a log line or
// an error message.
func DistributionName(id string) string {
	for _, dist := range distributions {
		if dist.ID == id {
			return dist.Name
		}
	}
	return id
}

// ResolveDistribution normalises a requested distribution id. An empty one is
// the default; anything unrecognised is refused rather than quietly turned
// into one, because silently installing a different vendor's Java than what
// was asked for is the surprise this axis exists to remove.
func ResolveDistribution(id string) (string, error) {
	if id == "" {
		return DefaultDistribution, nil
	}
	for _, dist := range distributions {
		if dist.ID == id {
			return id, nil
		}
	}
	return "", fmt.Errorf("%w: %q", ErrUnknownDistribution, id)
}
```

- [ ] **Step 4: `Release` 加字段，Adoptium 填上**

`adoptium.go` 的 `Release` 结构体最前面加：

```go
// Release is a specific build of a major version, for one platform.
type Release struct {
	// Distribution is who built it. It rides along on the release so that
	// everything downstream — the source list, the install id, the error
	// messages — can ask the release instead of taking another parameter.
	Distribution string `json:"distribution"`
	Major        int    `json:"major"`
	// ... 其余字段不动
}
```

`adoptiumProvider.LatestRelease` 的返回值里加 `Distribution: DistTemurin,`。

- [ ] **Step 5: `installID` 用发行版前缀**

`installer.go`，把最后一行的 `"temurin-"` 换掉，并更新注释：

```go
func installID(release Release) string {
	// Adoptium reports 21.0.12+8-LTS; the support status is not part of the
	// version and only makes the directory name harder to read.
	version := release.Version
	if trimmed := strings.TrimSuffix(strings.ToUpper(version), "-LTS"); len(trimmed) != len(version) {
		version = version[:len(trimmed)]
	}
	version = strings.Map(func(r rune) rune {
		switch {
		case r >= '0' && r <= '9', r >= 'a' && r <= 'z', r >= 'A' && r <= 'Z', r == '.':
			return r
		default:
			return '-'
		}
	}, version)
	version = strings.Trim(version, "-.")
	if version == "" {
		version = fmt.Sprintf("%d", release.Major)
	}
	// The distribution leads the name so two vendors' builds of the same
	// version can sit side by side. Runtimes installed before this existed are
	// all "temurin-…" and keep working: the store reads their release file,
	// not their directory name.
	return release.Distribution + "-" + version + "-" + release.ImageType
}
```

- [ ] **Step 6: `Installer.Start` 与 `Job` 带上发行版**

`Job` 结构体，在 `Major` 前面加：

```go
// Distribution is who built the runtime being installed.
Distribution string `json:"distribution"`
```

`Start` 的签名和开头：

```go
// Start resolves a build and begins installing it in the background.
//
// dist names the OpenJDK distribution and source names where the archive comes
// from; empty means the default and SourceAuto. The source has no bearing on
// which build gets installed — that comes from the distribution's metadata API
// either way — only on where the bytes are pulled from.
func (i *Installer) Start(dist string, major int, imageType, source string) (Job, error) {
	platform, err := CurrentPlatform()
	if err != nil {
		return Job{}, err
	}
	dist, err = ResolveDistribution(dist)
	if err != nil {
		return Job{}, err
	}
	source, err = ResolveSource(dist, source)
	if err != nil {
		return Job{}, err
	}
	// ...
	i.job = &Job{
		Distribution: dist,
		Major:        major,
		ImageType:    imageType,
		Source:       source,
		State:        JobDownloading,
		StartedAt:    time.Now(),
	}
```

`i.resolve(ctx, major, imageType, platform)` 改成 `i.resolve(ctx, dist, major, imageType, platform)`，`resolve` 内部转调 `i.client.LatestRelease(lookupCtx, dist, major, imageType, platform)`。日志行加 `"dist", dist`。

> `ResolveSource(dist, source)` 是 Task 4 的产物。**本任务先按两参数写**，Task 4 落地前 `go build` 会报错——所以 Task 2 和 Task 4 之间不要停在半路提交。若要严格按序，本步先写 `ResolveSource(source)` 单参数，Task 4 再改；下面的命令按后者给。

- [ ] **Step 7: 修调用点并跑测试**

`internal/api/handlers_java.go` 里 `s.java.Start(req.Major, req.ImageType, source)` 暂时改成 `s.java.Start("", req.Major, req.ImageType, source)`（Task 7 会换成真实的发行版）。

Run: `gofmt -l ./internal ./cmd && go test ./internal/javaruntime/ -run 'Distribution|InstallID' -v 2>&1 | tail -20`
Expected: 两个测试 PASS。

Run: `make test 2>&1 | tail -20`
Expected: 全绿。

- [ ] **Step 8: 提交**

```bash
git add internal/javaruntime internal/api/handlers_java.go
git commit -m "javaruntime：加入发行版这根轴，默认 Zulu

安装目录名从写死的 temurin- 前缀改成发行版 id，两个厂商的同一个版本
才能并排放着。已装的 temurin-* 目录不迁移也不改名——store 判版本靠读
目录里的 release 文件，跟目录名无关，改名反而会打断所有指着旧路径的
实例启动配置。

发行版跟着 Release 走而不是一路加形参：源表、安装 id、错误消息都能
问 release 自己。"
```

---

## Task 3: `IMAGE_TYPE` 缺席时靠 bin/javac 判定

Zulu 的 `release` 文件没有 `IMAGE_TYPE`（下真包验过），而 `applyReleaseFile` 只认这个键，照现状 Zulu 运行时的 `ImageType` 会是空串，`JavaPage.tsx` 那个 `runtime.imageType.toUpperCase()` 会渲染出空徽章。

**Files:**
- Modify: `internal/javaruntime/store.go`
- Modify: `internal/javaruntime/platform.go`（加 `javacBinary()`）
- Test: `internal/javaruntime/store_test.go`

**Interfaces:**
- Produces: `javacBinary() string`；`inspect` 在 `ImageType` 为空时回落。

- [ ] **Step 1: 写失败的测试**

加进 `store_test.go`：

```go
// Zulu's release file carries no IMAGE_TYPE. Without a fallback the panel
// would show an empty badge next to every Zulu runtime.
func TestInspectFallsBackToJavacForImageType(t *testing.T) {
	root := t.TempDir()
	for _, tc := range []struct {
		id      string
		javac   bool
		want    string
	}{
		{id: "zulu-21.0.12.1-jre", javac: false, want: ImageJRE},
		{id: "zulu-21.0.12.1-jdk", javac: true, want: ImageJDK},
	} {
		bin := filepath.Join(root, tc.id, "bin")
		if err := os.MkdirAll(bin, 0o755); err != nil {
			t.Fatal(err)
		}
		// A Zulu release file: IMPLEMENTOR and JAVA_VERSION but no IMAGE_TYPE.
		release := "IMPLEMENTOR=\"Azul Systems, Inc.\"\nJAVA_VERSION=\"21.0.12.1\"\n"
		if err := os.WriteFile(filepath.Join(root, tc.id, "release"), []byte(release), 0o644); err != nil {
			t.Fatal(err)
		}
		if err := os.WriteFile(filepath.Join(bin, javaBinary()), []byte("#!/bin/sh\n"), 0o755); err != nil {
			t.Fatal(err)
		}
		if tc.javac {
			if err := os.WriteFile(filepath.Join(bin, javacBinary()), []byte("#!/bin/sh\n"), 0o755); err != nil {
				t.Fatal(err)
			}
		}

		runtime, err := NewStore(root).Get(tc.id)
		if err != nil {
			t.Fatalf("Get(%s): %v", tc.id, err)
		}
		if runtime.ImageType != tc.want {
			t.Errorf("%s: image type = %q, want %q", tc.id, runtime.ImageType, tc.want)
		}
		if runtime.Vendor != "Azul Systems, Inc." {
			t.Errorf("%s: vendor = %q", tc.id, runtime.Vendor)
		}
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/javaruntime/ -run TestInspectFallsBackToJavac -v 2>&1 | tail -15`
Expected: 编译失败 `undefined: javacBinary`。

- [ ] **Step 3: 实现**

`platform.go`，紧挨着 `javaBinary()`：

```go
// javacBinary is the compiler's name inside a runtime's bin directory. Its
// presence is what actually separates a JDK from a JRE.
func javacBinary() string {
	if runtime.GOOS == "windows" {
		return "javac.exe"
	}
	return "javac"
}
```

`store.go` 的 `inspect`，在 `applyReleaseFile(&runtime, dir)` 之后、`if runtime.Version == ""` 之前插入：

```go
	// Zulu's release file carries no IMAGE_TYPE, and a runtime somebody
	// unpacked here by hand may carry no release file at all. javac is the
	// actual difference between the two images, so ask the directory rather
	// than the metadata — that also covers the builds the panel never fetched.
	if runtime.ImageType == "" && runtime.JavaPath != "" {
		if _, err := os.Stat(filepath.Join(filepath.Dir(runtime.JavaPath), javacBinary())); err == nil {
			runtime.ImageType = ImageJDK
		} else {
			runtime.ImageType = ImageJRE
		}
	}
```

- [ ] **Step 4: 跑测试**

Run: `go test ./internal/javaruntime/ -run TestInspect -v 2>&1 | tail -15`
Expected: PASS。

Run: `make test 2>&1 | tail -10`
Expected: 全绿（尤其 Temurin 的 `IMAGE_TYPE="JRE"` 仍然走原路径，不被回落覆盖）。

- [ ] **Step 5: 提交**

```bash
git add internal/javaruntime
git commit -m "javaruntime：release 文件没有 IMAGE_TYPE 时靠 javac 判定

Zulu 的 release 文件里没有 IMAGE_TYPE（下真包确认过），而
applyReleaseFile 只认这个键，于是 ImageType 会是空串，已装列表上那个
徽章会渲染成一个空壳。

判定改成看 bin/javac 在不在——那才是 JDK 和 JRE 的实际区别，既不依赖
发行版也不依赖目录名，顺带把手工解压进来的运行时也认对了。"
```

---

## Task 4: 下载源分表 + Zulu 自定义前缀

**Files:**
- Modify: `internal/javaruntime/source.go`
- Modify: `internal/javaruntime/source_test.go`
- Modify: `internal/javaruntime/installer.go`（`SourceName` 调用点）
- Modify: `internal/javaruntime/client.go`（已在 Task 1 按新签名写好）

**Interfaces:**
- Consumes: Task 2 的 `DistZulu` / `DistTemurin` / `Release.Distribution`。
- Produces: `Sources(dist string) []Source`；`SourceName(dist, id string) string`；`ResolveSource(dist, id string) (string, error)`；`attempts(dist, id string, release Release) []attempt`。

- [ ] **Step 1: 写失败的测试**

加进 `source_test.go`：

```go
func zuluRelease() Release {
	return Release{
		Distribution: DistZulu,
		Major:        21,
		ImageType:    ImageJRE,
		OS:           "linux",
		Arch:         "x64",
		FileName:     "zulu21.52.203-ca-jre21.0.12.1-linux_x64.tar.gz",
		URL:          "https://cdn.azul.com/zulu/bin/zulu21.52.203-ca-jre21.0.12.1-linux_x64.tar.gz",
	}
}

// Zulu's CDN lays its archives out flat, so a custom mirror only needs the
// file name appended — unlike Adoptium, whose mirrors copy a nested tree.
func TestZuluCustomPrefixIsPrefixPlusFileName(t *testing.T) {
	tries := attempts(DistZulu, "https://mirror.example/zulu/bin/", zuluRelease())
	if len(tries) != 2 {
		t.Fatalf("expected the custom prefix then the official link, got %+v", tries)
	}
	want := "https://mirror.example/zulu/bin/zulu21.52.203-ca-jre21.0.12.1-linux_x64.tar.gz"
	if tries[0].url != want {
		t.Errorf("custom link = %q, want %q", tries[0].url, want)
	}
	if tries[1].id != SourceOfficial {
		t.Errorf("the official link should still be the fallback, got %q", tries[1].id)
	}
}

// A prefix without its trailing slash is the easy typo; accepting it costs one
// line and saves a 404 that looks like a broken mirror.
func TestResolveSourceNormalisesACustomPrefix(t *testing.T) {
	got, err := ResolveSource(DistZulu, "https://mirror.example/zulu/bin")
	if err != nil {
		t.Fatalf("ResolveSource: %v", err)
	}
	if got != "https://mirror.example/zulu/bin/" {
		t.Errorf("resolved to %q", got)
	}
}

// The Adoptium mirrors carry no Zulu and vice versa, so each distribution gets
// its own list.
func TestSourcesAreScopedToTheDistribution(t *testing.T) {
	for _, id := range []string{"tuna", "nju", "huawei", "ghproxy"} {
		if _, err := ResolveSource(DistZulu, id); !errors.Is(err, ErrUnknownSource) {
			t.Errorf("%q should not be a Zulu source, got err %v", id, err)
		}
	}
	if _, err := ResolveSource(DistTemurin, "tuna"); err != nil {
		t.Errorf("tuna should still be a Temurin source: %v", err)
	}
	// Both lists share the two ids that mean the same thing everywhere.
	for _, dist := range []string{DistZulu, DistTemurin} {
		for _, id := range []string{SourceAuto, SourceOfficial} {
			if _, err := ResolveSource(dist, id); err != nil {
				t.Errorf("%s/%s: %v", dist, id, err)
			}
		}
	}
}

// Zulu downloads do not come off GitHub, so the GitHub proxy has nothing to
// offer and must not appear in its list.
func TestZuluAutoIsJustTheOfficialCDN(t *testing.T) {
	tries := attempts(DistZulu, SourceAuto, zuluRelease())
	if len(tries) != 1 || tries[0].id != SourceOfficial {
		t.Fatalf("expected only the official CDN, got %+v", tries)
	}
}
```

`source_test.go` 里既有的 `testRelease` 要补上发行版，否则它的 `attempts` 会走到 Zulu 表：

```go
func testRelease(link string) Release {
	return Release{
		Distribution: DistTemurin,
		Major:        21,
		// ... 其余不动
	}
}
```

既有的 `attempts(SourceAuto, ...)` / `attempts(SourceOfficial, ...)` 调用全部补第一个实参 `DistTemurin`；`len(mirrors)` 换成 `len(temurinSources)`。

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/javaruntime/ -run 'Source|Zulu' -v 2>&1 | tail -20`
Expected: 编译失败（`ResolveSource` 参数个数不对、`temurinSources` 未定义）。

- [ ] **Step 3: 改 `source.go`**

把 `mirrors` 改名 `temurinSources`，新增 `zuluSources`，并改四个函数的签名：

```go
// temurinSources are tried in this order by SourceAuto, and offered to the
// operator in this order too. The China mirrors come first because that is
// where the problem is: everywhere else the official link is already the fast
// one, and picking it is one click away.
var temurinSources = []source{
	// ...现有五项原样不动...
}

// zuluSources is short for a reason: Zulu's archives come off cdn.azul.com,
// not GitHub, so none of the Adoptium mirrors above carries a byte of it and
// the GitHub proxy has nothing to wrap. No Chinese mirror of the Zulu tree
// could be confirmed to exist (NJU, Aliyun, BFSU and Tencent all answer 404),
// so rather than guess at one, an operator who knows a working accelerator
// fills it in as a custom prefix — see ResolveSource.
var zuluSources = []source{
	{
		Source: Source{
			ID:   SourceOfficial,
			Name: "Azul 官方 CDN",
			Note: "cdn.azul.com 直连，商业 CDN，国内一般比 GitHub 好走",
		},
		link: func(release Release) string { return release.URL },
	},
}

// sourcesFor is the list a distribution downloads from.
func sourcesFor(dist string) []source {
	if dist == DistTemurin {
		return temurinSources
	}
	return zuluSources
}
```

`Sources` / `SourceName` / `ResolveSource` / `attempts` / `officialSource` 全部接 `dist`：

```go
// Sources lists what an operator can pick for a distribution, automatic first.
func Sources(dist string) []Source {
	list := sourcesFor(dist)
	out := make([]Source, 0, len(list)+1)
	out = append(out, autoSource)
	for _, entry := range list {
		out = append(out, entry.Source)
	}
	return out
}

// SourceName is the human name of a source id, for a log line or an error.
func SourceName(dist, id string) string {
	if id == SourceAuto || id == "" {
		return autoSource.Name
	}
	for _, entry := range sourcesFor(dist) {
		if entry.ID == id {
			return entry.Name
		}
	}
	// A custom prefix is its own name; there is nothing better to call it.
	return id
}

// ResolveSource normalises a requested source id for a distribution. An empty
// one is the default; anything unrecognised is refused rather than quietly
// turned into a default, because silently downloading from somewhere other
// than what was asked for is exactly the surprise this feature exists to
// remove.
//
// Zulu additionally accepts a custom "https://…/" prefix, the same way
// plugin.ResolveMirror does: no mirror of the Zulu tree could be confirmed to
// exist in China, and an operator who has found one — or runs their own — is
// exactly the person this should not stand in the way of.
func ResolveSource(dist, id string) (string, error) {
	id = strings.TrimSpace(id)
	switch id {
	case "", SourceAuto:
		return SourceAuto, nil
	}
	for _, entry := range sourcesFor(dist) {
		if entry.ID == id {
			return id, nil
		}
	}
	if dist == DistZulu && (strings.HasPrefix(id, "https://") || strings.HasPrefix(id, "http://")) {
		if !strings.HasSuffix(id, "/") {
			id += "/"
		}
		return id, nil
	}
	return "", fmt.Errorf("%w: %q", ErrUnknownSource, id)
}
```

`attempts` 加发行版，并让自定义前缀走一条自己的分支：

```go
func attempts(dist, id string, release Release) []attempt {
	list := sourcesFor(dist)

	var chosen []source
	switch id {
	case SourceAuto, "":
		chosen = list
	default:
		for _, entry := range list {
			if entry.ID == id {
				chosen = []source{entry}
				break
			}
		}
		if len(chosen) == 0 && dist == DistZulu && strings.HasPrefix(id, "http") {
			chosen = []source{{
				Source: Source{ID: id, Name: id},
				link:   flatLink(id),
			}}
		}
		if id != SourceOfficial {
			chosen = append(chosen, officialSource(dist))
		}
	}

	out := make([]attempt, 0, len(chosen))
	seen := make(map[string]bool, len(chosen))
	for _, entry := range chosen {
		link := entry.link(release)
		if link == "" || seen[link] {
			continue
		}
		seen[link] = true
		out = append(out, attempt{id: entry.ID, url: link})
	}
	return out
}

func officialSource(dist string) source {
	for _, entry := range sourcesFor(dist) {
		if entry.ID == SourceOfficial {
			return entry
		}
	}
	panic("javaruntime: the official source is missing from the source list")
}

// flatLink builds the path a mirror of Zulu's CDN stores a build under. Unlike
// Adoptium's nested tree, cdn.azul.com/zulu/bin holds every archive in one
// directory, so the file name is the whole path.
func flatLink(base string) func(Release) string {
	if !strings.HasSuffix(base, "/") {
		base += "/"
	}
	return func(release Release) string {
		if release.FileName == "" {
			return ""
		}
		return base + url.PathEscape(release.FileName)
	}
}
```

文件顶部那段说明也要改——它现在讲的全是 Adoptium。把「Download sources」那段注释改写成两张表的由来，把「The mirrors below are byte-for-byte rsync copies of Adoptium's release tree」这句挪到 `temurinSources` 头上。

- [ ] **Step 4: 修 `installer.go` 里写死 Adoptium 的地方**

`download` 里的 `from := SourceName(served)` 改成 `from := SourceName(release.Distribution, served)`。

同一个文件里还有三处注释和一条错误说的是 Adoptium，现在有两个发行版了，改成中性说法：

```go
	// ErrChecksum is recorded when the archive is not what the distribution
	// published.
	ErrChecksum = errors.New("checksum mismatch")
```

`download` 头上那段注释同理——把「what Adoptium published」「comes from the Adoptium API」改成
「what the distribution published」「comes from the distribution's metadata API」。**那句「never from
the source serving the file」要留着**，它记的是这套设计为什么安全。

`Start` 的注释里「that comes from the Adoptium API either way」在 Task 2 已经改过，确认一下没漏。

- [ ] **Step 5: 跑测试**

Run: `gofmt -l ./internal && go test ./internal/javaruntime/ -v 2>&1 | tail -25`
Expected: 全绿，含四个新测试。

- [ ] **Step 6: 提交**

```bash
git add internal/javaruntime
git commit -m "javaruntime：下载源按发行版分表，Zulu 支持自定义前缀

现有五个源全部是围绕 Adoptium 发布树建的：三个镜像是它的 rsync 副本，
ghfast 只代理 github.com 开头的链接。Zulu 走 cdn.azul.com，这五个一个
都用不上，混在一张表里只会让人选了之后拿到 404。

国内没有能确认存在的 Zulu 镜像（南大、阿里、北外、腾讯实测 404），所以
不猜，改成官方 CDN 加一个自填前缀——形状照 plugin.ResolveMirror。Zulu 的
CDN 是扁平目录，前缀拼文件名就够。"
```

---

## Task 5: Azul provider

**Files:**
- Create: `internal/javaruntime/azul.go`
- Create: `internal/javaruntime/azul_test.go`

**Interfaces:**
- Consumes: Task 1 的 `httpClient` / `majorCache`、Task 2 的 `provider` 接口与 `DistZulu`。
- Produces: `newAzulProvider(baseURL string, client *httpClient) *azulProvider`，满足 `provider`。

- [ ] **Step 1: 写失败的测试**

`internal/javaruntime/azul_test.go`：

```go
package javaruntime

import (
	"context"
	"fmt"
	"net/http"
	"net/http/httptest"
	"testing"
)

// azulPayload is one package as the metadata API returns it, with the fields
// include_fields adds. java_version is an array, not a string — that is the
// main shape difference from Adoptium.
func azulPayload(link, checksum string, size int64) string {
	return fmt.Sprintf(`[{
	  "package_uuid":"f89c621b-7e5d-4147-b7f7-8dc832ad2413",
	  "name":"zulu21.52.203-ca-jre21.0.12.1-linux_x64.tar.gz",
	  "java_version":[21,0,12,1],
	  "distro_version":[21,52,203,0],
	  "download_url":%q,
	  "sha256_hash":%q,
	  "size":%d,
	  "java_package_type":"jre",
	  "os":"linux",
	  "arch":"x86",
	  "lib_c_type":"glibc",
	  "availability_type":"CA",
	  "latest":true
	}]`, link, checksum, size)
}

func TestAzulLatestReleaseParsesAPackage(t *testing.T) {
	upstream := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		query := r.URL.Query()
		// These two keep CRaC builds and JavaFX-bundled builds out. Without
		// them the API really does hand back a crac jre; see the spec.
		if got := query.Get("crac_supported"); got != "false" {
			t.Errorf("crac_supported = %q, want false", got)
		}
		if got := query.Get("javafx_bundled"); got != "false" {
			t.Errorf("javafx_bundled = %q, want false", got)
		}
		if got := query.Get("release_status"); got != "ga" {
			t.Errorf("release_status = %q, want ga", got)
		}
		if got := query.Get("java_package_type"); got != "jre" {
			t.Errorf("java_package_type = %q", got)
		}
		if got := query.Get("java_version"); got != "21" {
			t.Errorf("java_version = %q", got)
		}
		if got := query.Get("archive_type"); got != "tar.gz" {
			t.Errorf("archive_type = %q, want tar.gz", got)
		}
		if got := query.Get("lib_c_type"); got != "glibc" {
			t.Errorf("lib_c_type = %q", got)
		}
		fmt.Fprint(w, azulPayload("https://cdn.example/zulu.tar.gz", "AABBCC", 4096))
	}))
	defer upstream.Close()

	release, err := newAzulProvider(upstream.URL, newHTTPClient("test")).
		LatestRelease(context.Background(), 21, ImageJRE, Platform{OS: "linux", Arch: "x64", LibC: "glibc"})
	if err != nil {
		t.Fatalf("LatestRelease: %v", err)
	}
	if release.Distribution != DistZulu {
		t.Errorf("distribution = %q", release.Distribution)
	}
	if release.Version != "21.0.12.1" {
		t.Errorf("version = %q, want 21.0.12.1", release.Version)
	}
	if release.Major != 21 || release.Size != 4096 {
		t.Errorf("unexpected release: %+v", release)
	}
	if release.SHA256 != "aabbcc" {
		t.Errorf("checksum should be lowercased, got %q", release.SHA256)
	}
	if release.FileName != "zulu21.52.203-ca-jre21.0.12.1-linux_x64.tar.gz" {
		t.Errorf("file name = %q", release.FileName)
	}
	// The response says "x86" for a 64-bit build. Trusting it would put the
	// wrong arch on the runtime and on every error message about it.
	if release.Arch != "x64" {
		t.Errorf("arch = %q, want the requested x64 rather than the response's", release.Arch)
	}
}

// A musl machine needs the musl build, which is the whole reason Zulu is worth
// having on Alpine.
func TestAzulAsksForMuslOnAMuslMachine(t *testing.T) {
	upstream := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if got := r.URL.Query().Get("lib_c_type"); got != "musl" {
			t.Errorf("lib_c_type = %q, want musl", got)
		}
		fmt.Fprint(w, azulPayload("https://cdn.example/zulu-musl.tar.gz", "AABBCC", 4096))
	}))
	defer upstream.Close()

	if _, err := newAzulProvider(upstream.URL, newHTTPClient("test")).
		LatestRelease(context.Background(), 21, ImageJRE, Platform{OS: "linux", Arch: "x64", LibC: "musl"}); err != nil {
		t.Fatalf("LatestRelease: %v", err)
	}
}

// Windows ships as a zip; asking for tar.gz there is an empty result.
func TestAzulAsksForZipOnWindows(t *testing.T) {
	upstream := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if got := r.URL.Query().Get("archive_type"); got != "zip" {
			t.Errorf("archive_type = %q, want zip", got)
		}
		fmt.Fprint(w, `[{"name":"zulu21.52.203-ca-jre21.0.12.1-win_x64.zip",
		  "java_version":[21,0,12,1],"download_url":"https://cdn.example/zulu.zip",
		  "sha256_hash":"aabbcc","size":4096}]`)
	}))
	defer upstream.Close()

	release, err := newAzulProvider(upstream.URL, newHTTPClient("test")).
		LatestRelease(context.Background(), 21, ImageJRE, Platform{OS: "windows", Arch: "x64", LibC: "glibc"})
	if err != nil {
		t.Fatalf("LatestRelease: %v", err)
	}
	if release.FileName != "zulu21.52.203-ca-jre21.0.12.1-win_x64.zip" {
		t.Errorf("file name = %q", release.FileName)
	}
}

func TestAzulMajorsFlagsLTSAndCaches(t *testing.T) {
	var hits int
	upstream := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		hits++
		fmt.Fprint(w, `[{"java_version":[25,0,1],"support_term":"lts"},
		  {"java_version":[24,0,2],"support_term":"mts"},
		  {"java_version":[21,0,12,1],"support_term":"lts"},
		  {"java_version":[21,0,11],"support_term":"lts"}]`)
	}))
	defer upstream.Close()

	azul := newAzulProvider(upstream.URL, newHTTPClient("test"))
	majors, err := azul.Majors(context.Background())
	if err != nil {
		t.Fatalf("Majors: %v", err)
	}
	// Newest first, one entry per feature release even though the API lists
	// several builds of 21.
	if len(majors) != 3 || majors[0].Major != 25 || majors[2].Major != 21 {
		t.Fatalf("unexpected majors: %+v", majors)
	}
	if !majors[0].LTS || majors[1].LTS {
		t.Errorf("LTS flags wrong: %+v", majors)
	}

	if _, err := azul.Majors(context.Background()); err != nil {
		t.Fatalf("second Majors: %v", err)
	}
	if hits != 1 {
		t.Errorf("upstream hit %d times, want 1 (cached)", hits)
	}
}

func TestAzulReportsNothingMatching(t *testing.T) {
	upstream := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		fmt.Fprint(w, `[]`)
	}))
	defer upstream.Close()

	_, err := newAzulProvider(upstream.URL, newHTTPClient("test")).
		LatestRelease(context.Background(), 99, ImageJRE, Platform{OS: "linux", Arch: "x64", LibC: "glibc"})
	if err == nil {
		t.Fatal("expected an error for a version Azul does not build")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/javaruntime/ -run TestAzul -v 2>&1 | tail -15`
Expected: 编译失败 `undefined: newAzulProvider`。

- [ ] **Step 3: 写 `azul.go`**

```go
package javaruntime

import (
	"context"
	"fmt"
	"net/url"
	"sort"
	"strconv"
	"strings"
)

// defaultAzulBaseURL is Azul's metadata API. Zulu is a TCK-certified OpenJDK
// build with no account and no click-through licence, and every package comes
// with a SHA-256 the panel checks before unpacking anything.
const defaultAzulBaseURL = "https://api.azul.com/metadata/v1/zulu"

// azulProvider serves Azul Zulu.
type azulProvider struct {
	baseURL string
	http    *httpClient
	cache   majorCache
}

func newAzulProvider(baseURL string, client *httpClient) *azulProvider {
	if baseURL == "" {
		baseURL = defaultAzulBaseURL
	}
	return &azulProvider{baseURL: strings.TrimSuffix(baseURL, "/"), http: client}
}

// azulPackage is one entry of the packages listing. Only the fields the panel
// uses are named; sha256_hash and size arrive because the query asks for them
// with include_fields, which saves a second request per install.
type azulPackage struct {
	Name        string `json:"name"`
	JavaVersion []int  `json:"java_version"`
	DownloadURL string `json:"download_url"`
	SHA256      string `json:"sha256_hash"`
	Size        int64  `json:"size"`
	SupportTerm string `json:"support_term"`
}

// version renders 21.0.12.1 out of the array the API reports.
func (p azulPackage) version() string {
	parts := make([]string, 0, len(p.JavaVersion))
	for _, n := range p.JavaVersion {
		parts = append(parts, strconv.Itoa(n))
	}
	return strings.Join(parts, ".")
}

// baseQuery is everything both endpoints have to say to get a plain, current,
// generally-available Zulu and nothing else.
//
// crac_supported and javafx_bundled are not optional: left off, the API really
// does return CRaC builds and JavaFX-bundled ones mixed in with the plain
// packages, and the panel would install whichever sorted first.
func baseQuery() url.Values {
	return url.Values{
		"availability_type": {"ca"},
		"release_status":    {"ga"},
		"javafx_bundled":    {"false"},
		"crac_supported":    {"false"},
	}
}

// archiveType is what this OS ships as. Windows is a zip; everything else is a
// gzipped tarball, and asking for the wrong one is an empty result rather than
// an error.
func archiveType(osName string) string {
	if osName == "windows" {
		return "zip"
	}
	return "tar.gz"
}

func (p *azulProvider) Majors(ctx context.Context) ([]Major, error) {
	if cached, ok := p.cache.get(); ok {
		return cached, nil
	}

	query := baseQuery()
	query.Set("java_package_type", ImageJRE)
	query.Set("latest", "true")
	query.Set("page_size", "200")
	query.Set("include_fields", "support_term")

	var payload []azulPackage
	if err := p.http.getJSON(ctx, p.baseURL+"/packages/?"+query.Encode(), &payload); err != nil {
		return nil, err
	}
	if len(payload) == 0 {
		return nil, fmt.Errorf("%w: no releases listed", ErrUpstream)
	}

	// The listing is per package, so one feature release shows up many times
	// over — once per build and platform. Fold it down to the feature number.
	lts := make(map[int]bool)
	for _, entry := range payload {
		if len(entry.JavaVersion) == 0 {
			continue
		}
		major := entry.JavaVersion[0]
		if _, seen := lts[major]; !seen || strings.EqualFold(entry.SupportTerm, "lts") {
			lts[major] = strings.EqualFold(entry.SupportTerm, "lts")
		}
	}
	if len(lts) == 0 {
		return nil, fmt.Errorf("%w: no usable releases listed", ErrUpstream)
	}

	majors := make([]Major, 0, len(lts))
	for major, isLTS := range lts {
		majors = append(majors, Major{Major: major, LTS: isLTS})
	}
	sort.Slice(majors, func(a, b int) bool { return majors[a].Major > majors[b].Major })

	p.cache.put(majors)
	return majors, nil
}

func (p *azulProvider) LatestRelease(ctx context.Context, major int, imageType string, platform Platform) (Release, error) {
	query := baseQuery()
	query.Set("java_version", strconv.Itoa(major))
	query.Set("java_package_type", imageType)
	query.Set("os", platform.OS)
	query.Set("arch", platform.Arch)
	query.Set("archive_type", archiveType(platform.OS))
	query.Set("latest", "true")
	query.Set("page_size", "1")
	query.Set("include_fields", "sha256_hash,size")
	if platform.LibC != "" {
		query.Set("lib_c_type", platform.LibC)
	}

	var payload []azulPackage
	if err := p.http.getJSON(ctx, p.baseURL+"/packages/?"+query.Encode(), &payload); err != nil {
		return Release{}, err
	}
	if len(payload) == 0 {
		return Release{}, fmt.Errorf("%w: Azul 没有 Java %d 的 %s（%s/%s%s）",
			ErrUnknownRelease, major, imageType, platform.OS, platform.Arch, libcSuffix(platform.LibC))
	}

	entry := payload[0]
	name, err := safeFileName(entry.Name)
	if err != nil {
		return Release{}, err
	}
	if err := p.http.checkDownloadURL(entry.DownloadURL); err != nil {
		return Release{}, err
	}
	if !isSupportedArchive(name) {
		return Release{}, fmt.Errorf("%w: 不认识的包格式 %q", ErrUpstream, name)
	}
	version := entry.version()
	if version == "" {
		return Release{}, fmt.Errorf("%w: package %q reports no java version", ErrUpstream, name)
	}

	return Release{
		Distribution: DistZulu,
		Major:        major,
		Version:      version,
		Name:         strings.TrimSuffix(strings.TrimSuffix(name, ".tar.gz"), ".zip"),
		ImageType:    imageType,
		// The platform comes from the request, never from the response: Azul
		// reports "x86" with hw_bitness 64 for an x64 build and "arm" for
		// aarch64, so its own arch field would put the wrong label on the
		// runtime and on every error message about it.
		OS:       platform.OS,
		Arch:     platform.Arch,
		FileName: name,
		URL:      entry.DownloadURL,
		SHA256:   strings.ToLower(entry.SHA256),
		Size:     entry.Size,
	}, nil
}

func libcSuffix(libc string) string {
	if libc == "" || libc == "glibc" {
		return ""
	}
	return ", " + libc
}
```

- [ ] **Step 4: 跑测试**

Run: `gofmt -l ./internal && go test ./internal/javaruntime/ -run TestAzul -v 2>&1 | tail -25`
Expected: 五个测试全 PASS。

Run: `make test 2>&1 | tail -10`
Expected: 全绿。

- [ ] **Step 5: 提交**

```bash
git add internal/javaruntime
git commit -m "javaruntime：接上 Azul 的元数据接口

一次请求拿齐 download_url、sha256_hash 和 size（靠 include_fields），
校验链跟 Adoptium 那条一模一样：字节先落盘，比对通过才解压。

三处是实测踩出来的，不写就会中招：必须显式 crac_supported=false 和
javafx_bundled=false，否则 CRaC 和捆 JavaFX 的包会混进结果；响应里的
arch 字段查 x64 返回 x86、查 aarch64 返回 arm，一律用请求时的平台；
Windows 的包是 zip 不是 tar.gz。"
```

---

## Task 6: Platform.LibC 与按发行版算的警告

**Files:**
- Modify: `internal/javaruntime/platform.go`
- Test: `internal/javaruntime/platform_test.go`（新建，仓库暂无此文件）

**Interfaces:**
- Produces: `Platform.LibC`；`PlatformWarning(dist string, platform Platform) string`。`CurrentPlatform()` 不再填 `Warning`。

- [ ] **Step 1: 写失败的测试**

`internal/javaruntime/platform_test.go`：

```go
package javaruntime

import "testing"

// musl is only a problem for Temurin, which links against glibc. Zulu builds
// for musl, so on Alpine it is the answer rather than the casualty — and
// CurrentPlatform cannot know which one is selected, so the warning is
// computed per distribution instead of baked into the platform.
func TestPlatformWarningIsPerDistribution(t *testing.T) {
	musl := Platform{OS: "linux", Arch: "x64", LibC: "musl"}
	if got := PlatformWarning(DistZulu, musl); got != "" {
		t.Errorf("Zulu on musl should not warn, got %q", got)
	}
	if got := PlatformWarning(DistTemurin, musl); got == "" {
		t.Error("Temurin on musl should warn")
	}

	glibc := Platform{OS: "linux", Arch: "x64", LibC: "glibc"}
	for _, dist := range []string{DistZulu, DistTemurin} {
		if got := PlatformWarning(dist, glibc); got != "" {
			t.Errorf("%s on glibc should not warn, got %q", dist, got)
		}
	}
}

func TestCurrentPlatformReportsLibC(t *testing.T) {
	platform, err := CurrentPlatform()
	if err != nil {
		t.Skipf("no build for this platform: %v", err)
	}
	if platform.OS == "linux" && platform.LibC == "" {
		t.Error("a linux platform should say which libc it is")
	}
	// The warning is not the platform's to hold any more.
	if platform.Warning != "" {
		t.Errorf("CurrentPlatform should not fill Warning, got %q", platform.Warning)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/javaruntime/ -run Platform -v 2>&1 | tail -15`
Expected: 编译失败 `undefined: PlatformWarning`、`platform.LibC undefined`。

- [ ] **Step 3: 改 `platform.go`**

```go
// Platform is the OS/architecture pair to ask a distribution for.
type Platform struct {
	OS   string `json:"os"`
	Arch string `json:"arch"`
	// LibC is "glibc" or "musl" on Linux, empty elsewhere. Zulu builds for
	// both; Temurin only for glibc.
	LibC string `json:"libc,omitempty"`
	// Warning is a non-fatal note about installing on this platform. It
	// depends on which distribution is selected, so it is filled in by
	// PlatformWarning rather than by CurrentPlatform.
	Warning string `json:"warning,omitempty"`
}
```

`CurrentPlatform` 的最后一行改成：

```go
	return Platform{OS: osName, Arch: arch, LibC: detectLibC()}, nil
```

错误消息里的 "Adoptium" 改成中性说法：

```go
		return Platform{}, fmt.Errorf("%w: 没有 %s 的 Java 构建", ErrUnsupported, runtime.GOOS)
	}
	arch, ok := archNames[runtime.GOARCH]
	if !ok {
		return Platform{}, fmt.Errorf("%w: 没有 %s/%s 的 Java 构建", ErrUnsupported, runtime.GOOS, runtime.GOARCH)
```

`platformWarning` 换成这两个函数：

```go
// detectLibC says which C library this Linux runs. Alpine and friends ship
// musl, where a glibc-linked JDK unpacks fine and then fails to start with a
// dynamic-linker error.
func detectLibC() string {
	if runtime.GOOS != "linux" {
		return ""
	}
	matches, err := filepath.Glob("/lib/ld-musl-*.so.1")
	if err != nil || len(matches) == 0 {
		return "glibc"
	}
	return "musl"
}

// PlatformWarning flags a combination that would install fine and then fail to
// run. Better to say so before a 50 MB download than after.
//
// It takes the distribution because the only such combination left is
// Temurin's: it is a glibc build, so on musl every binary in the tarball is a
// dynamic-linker error waiting to happen. Zulu ships a musl build, which is
// why the way out is now a switch in the panel rather than a package manager.
func PlatformWarning(dist string, platform Platform) string {
	if dist != DistTemurin || platform.LibC != "musl" {
		return ""
	}
	return "这台机器用的是 musl（Alpine 之类），Temurin 是 glibc 构建，装上也跑不起来。" +
		"在「下载设置」里把发行版换成 Azul Zulu 就能装 —— 它有 musl 构建。"
}
```

- [ ] **Step 4: 跑测试**

Run: `gofmt -l ./internal && go test ./internal/javaruntime/ -run Platform -v 2>&1 | tail -15`
Expected: PASS。

Run: `make test 2>&1 | tail -10`
Expected: 全绿。

- [ ] **Step 5: 提交**

```bash
git add internal/javaruntime
git commit -m "javaruntime：区分 glibc 与 musl，警告改成按发行版算

musl 上装不了这件事只对 Temurin 成立，而 CurrentPlatform 不知道当前选
的是哪个发行版，没法自己决定要不要出这句话。所以平台只负责事实（OS、
arch、libc），「这个发行版在这个平台上行不行」交给 PlatformWarning。

Zulu 有 musl 构建，于是 Alpine 上的出路从「请用包管理器自己装」变成
「在下载设置里换个发行版」——这句话现在是真的了。"
```

---

## Task 7: API 与配置

**Files:**
- Modify: `internal/config/config.go`
- Modify: `internal/api/handlers_java.go`
- Modify: `internal/api/server_test.go`（fake Azul）
- Test: `internal/api/handlers_java_test.go`

**Interfaces:**
- Consumes: Task 2-6 全部。
- Produces: `javaOverview.Distributions` / `.Distribution`；`installJavaRequest.Distribution`；`GET /api/java/available?distribution=`；`config.Panel.JavaDistribution`。

- [ ] **Step 1: 写失败的测试**

加进 `handlers_java_test.go`。助手用文件里现成的那几个：`newTestEnv(t)` + `env.login()` 发请求靠
`env.do(method, path, body)`，读响应靠 `decodeBody(t, resp, &dst)`。

```go
// A panel that has never installed anything offers Zulu, and says so.
func TestJavaOverviewDefaultsToZulu(t *testing.T) {
	env := newTestEnv(t)
	env.login()

	var overview javaOverview
	decodeBody(t, env.do(http.MethodGet, "/api/java", nil), &overview)
	if overview.Distribution != javaruntime.DistZulu {
		t.Errorf("default distribution = %q, want %q", overview.Distribution, javaruntime.DistZulu)
	}
	if len(overview.Distributions) != 2 || overview.Distributions[0].ID != javaruntime.DistZulu {
		t.Errorf("unexpected distribution list: %+v", overview.Distributions)
	}
	// The source list belongs to the selected distribution: Zulu has no
	// Adoptium mirrors, so offering them here would be a guaranteed 404.
	for _, source := range overview.Sources {
		if source.ID == "tuna" {
			t.Error("the Adoptium mirrors must not be offered for Zulu")
		}
	}
}

// The distribution an install named is remembered, the same way the source is.
func TestInstallRemembersTheDistribution(t *testing.T) {
	env := newTestEnv(t)
	env.login()

	resp := env.do(http.MethodPost, "/api/java/install", installJavaRequest{
		Distribution: javaruntime.DistTemurin,
		Major:        21,
		ImageType:    "jre",
		Source:       javaruntime.SourceOfficial,
	})
	if resp.StatusCode != http.StatusAccepted {
		t.Fatalf("expected 202, got %d", resp.StatusCode)
	}
	if job := env.awaitInstall(); job.State != javaruntime.JobDone {
		t.Fatalf("install ended %s: %s", job.State, job.Error)
	}

	var overview javaOverview
	decodeBody(t, env.do(http.MethodGet, "/api/java", nil), &overview)
	if overview.Distribution != javaruntime.DistTemurin {
		t.Errorf("overview distribution = %q", overview.Distribution)
	}

	panel, err := env.store.LoadPanel()
	if err != nil {
		t.Fatalf("LoadPanel: %v", err)
	}
	if panel.JavaDistribution != javaruntime.DistTemurin {
		t.Errorf("panel.json holds %q", panel.JavaDistribution)
	}
}

// An unknown distribution is a bad request, not a silent fallback.
func TestInstallRefusesAnUnknownDistribution(t *testing.T) {
	env := newTestEnv(t)
	env.login()

	resp := env.do(http.MethodPost, "/api/java/install", installJavaRequest{
		Distribution: "graalvm", Major: 21, ImageType: "jre",
	})
	if resp.StatusCode != http.StatusBadRequest {
		t.Errorf("expected 400 for an unknown distribution, got %d", resp.StatusCode)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/api/ -run 'Java|Install' -v 2>&1 | tail -20`
Expected: 编译失败（`overview.Distribution` 未定义）。

- [ ] **Step 3: `config.go` 加字段**

紧挨着 `JavaSource` 加：

```go
	// JavaDistribution is which OpenJDK distribution new installs come from,
	// by the id of one of javaruntime's distributions. Empty means the
	// default, which is both what a config written before this setting existed
	// carries and what a fresh panel gets — so no pointer is needed to tell
	// them apart.
	//
	// Remembered from the last install rather than set on a settings page, for
	// the same reason JavaSource is: it describes this machine's situation,
	// not one download's.
	JavaDistribution string `json:"javaDistribution,omitempty"`
```

- [ ] **Step 4: `handlers_java.go`**

`javaOverview` 结构体加两个字段：

```go
	// Distributions are the OpenJDK builds an install can pick from, default
	// first. A fixed list like Sources, for the same reason.
	Distributions []javaruntime.Distribution `json:"distributions"`
	// Distribution is the one the last install used, which is what the page
	// preselects.
	Distribution string `json:"distribution"`
```

加两个方法，形状照抄紧邻的 `javaSource` / `rememberJavaSource`：

```go
// javaDistribution is the remembered distribution, or the default when nothing
// has been chosen yet — or when panel.json names one this build does not have.
func (s *Server) javaDistribution() string {
	s.panelMu.RLock()
	stored := s.panel.JavaDistribution
	s.panelMu.RUnlock()

	dist, err := javaruntime.ResolveDistribution(stored)
	if err != nil {
		return javaruntime.DefaultDistribution
	}
	return dist
}

// rememberJavaDistribution persists the distribution an install was started
// with. A failure to save is logged and otherwise ignored: the install is
// already running, and losing a preference is not worth failing it over.
func (s *Server) rememberJavaDistribution(dist string) {
	s.panelMu.Lock()
	if s.panel.JavaDistribution == dist {
		s.panelMu.Unlock()
		return
	}
	s.panel.JavaDistribution = dist
	panel := s.panel
	s.panelMu.Unlock()

	if err := s.store.SavePanel(panel); err != nil {
		s.log.Error("could not persist the java distribution", "err", err)
		return
	}
	s.log.Info("java distribution changed", "distribution", dist)
}
```

`javaSource()` 改成按发行版解析：

```go
func (s *Server) javaSource() string {
	s.panelMu.RLock()
	stored := s.panel.JavaSource
	s.panelMu.RUnlock()

	// A source remembered for one distribution means nothing to another: the
	// Adoptium mirrors carry no Zulu. Falling back to the automatic order is
	// the only honest reading of "清华" once the distribution has changed —
	// unlike an explicitly requested source, which is still refused outright.
	source, err := javaruntime.ResolveSource(s.javaDistribution(), stored)
	if err != nil {
		return javaruntime.SourceAuto
	}
	return source
}
```

`writeJavaError` 的第一个 case 里加上新错误：

```go
	case errors.Is(err, javaruntime.ErrUnknownRelease), errors.Is(err, javaruntime.ErrUnsupported),
		errors.Is(err, javaruntime.ErrUnknownSource), errors.Is(err, javaruntime.ErrUnknownDistribution):
		writeError(w, http.StatusBadRequest, err.Error())
```

`handleJavaOverview` 两处构造 `javaOverview` 的地方都补上：

```go
		Distributions: javaruntime.Distributions(),
		Distribution:  s.javaDistribution(),
		Sources:       javaruntime.Sources(s.javaDistribution()),
		Source:        s.javaSource(),
```

平台警告那段改成：

```go
	if platform, err := javaruntime.CurrentPlatform(); err == nil {
		platform.Warning = javaruntime.PlatformWarning(s.javaDistribution(), platform)
		overview.Platform = platform
	} else {
		overview.Platform = javaruntime.Platform{Warning: err.Error()}
	}
```

`handleListJavaMajors` 接受查询参数：

```go
	dist := r.URL.Query().Get("distribution")
	if dist == "" {
		dist = s.javaDistribution()
	}
	majors, err := s.java.Client().Majors(r.Context(), dist)
```

`installJavaRequest` 加字段并在 handler 里用上：

```go
type installJavaRequest struct {
	// Distribution names the OpenJDK build; empty is the remembered one.
	Distribution string `json:"distribution"`
	Major        int    `json:"major"`
	ImageType    string `json:"imageType"`
	// Source names where to download from; empty is the automatic choice.
	Source string `json:"source"`
}
```

```go
	// A request that names neither gets what the last install used, not the
	// built-in default: an operator who moved off them did so for a reason.
	dist := req.Distribution
	if dist == "" {
		dist = s.javaDistribution()
	}
	source := req.Source
	if source == "" && dist == s.javaDistribution() {
		source = s.javaSource()
	}

	job, err := s.java.Start(dist, req.Major, req.ImageType, source)
	if err != nil {
		s.writeJavaError(w, err)
		return
	}
	s.rememberJavaDistribution(job.Distribution)
	s.rememberJavaSource(job.Source)
```

> `source == "" && dist == s.javaDistribution()` 这个条件是有意的：切换发行版时不要把上一个发行版的源带过去，让它落到 `auto`。记忆写的是 `job` 上的值（`Start` 已经 resolve 过），不是请求里的原始串。

- [ ] **Step 5: `server_test.go` 加一个假 Azul**

`newTestEnv` 里把 java 客户端改成两边都指向假服务器：

```go
		Java: javaruntime.NewInstaller(
			javaruntime.NewClient("test", map[string]string{
				javaruntime.DistTemurin: adoptium.URL(),
				javaruntime.DistZulu:    azul.URL(),
			}),
			javaruntime.NewStore(paths.JavaRoot()),
```

`handlers_java_test.go` 里照着 `fakeAdoptium` 加一个 `fakeAzul`，路由 `GET /packages/` 返回 Azul 形状的单条结果，`GET /zulu.tar.gz` 返回同一个 `fake.archive`：

```go
// fakeAzul serves the Azul metadata shape over the same archive fakeAdoptium
// uses, so an install through either distribution exercises the same unpack.
type fakeAzul struct {
	server  *httptest.Server
	archive []byte
}

func (f *fakeAzul) URL() string { return f.server.URL }

func newFakeAzul(t *testing.T) *fakeAzul {
	t.Helper()
	fake := &fakeAzul{archive: fakeJREArchive(t)}

	mux := http.NewServeMux()
	mux.HandleFunc("GET /packages/", func(w http.ResponseWriter, r *http.Request) {
		sum := sha256.Sum256(fake.archive)
		fmt.Fprintf(w, `[{"name":"zulu21.52.203-ca-jre21.0.12.1-linux_x64.tar.gz",
			"java_version":[21,0,12,1],"download_url":"%s/zulu.tar.gz",
			"sha256_hash":%q,"size":%d,"support_term":"lts"}]`,
			fake.URL(), hex.EncodeToString(sum[:]), len(fake.archive))
	})
	mux.HandleFunc("GET /zulu.tar.gz", func(w http.ResponseWriter, r *http.Request) {
		w.Write(fake.archive)
	})

	fake.server = httptest.NewServer(mux)
	t.Cleanup(fake.server.Close)
	return fake
}
```

> 注意假 Azul 的 `release` 文件来自 `fakeJREArchive`，它带着 Temurin 的 `IMAGE_TYPE="JRE"`。这不影响本任务的断言；Task 3 的 javac 回落已有自己的单测覆盖。

现有那三个 install 测试（`handlers_java_test.go:149`、`182`、`213`、`274`）的请求体里都要补 `"distribution": javaruntime.DistTemurin`，否则它们会走 Zulu。

- [ ] **Step 6: 跑测试**

Run: `gofmt -l ./internal && go test ./internal/api/ -run Java -v 2>&1 | tail -25`
Expected: 全绿。

Run: `make lint && make test 2>&1 | tail -15`
Expected: 全绿。

- [ ] **Step 7: 提交**

```bash
git add internal/api internal/config
git commit -m "面板：Java 安装可以选发行版，默认 Zulu

三个端点各加一个字段，已有字段语义不动。发行版和下载源一样从上次安装
记忆，而不是做成设置页上的开关——它描述的是这台机器的处境，不是某一次
下载的。

跨发行版的源记忆有个例外要说明：面板记着「清华」、你切到 Zulu，这时不
报错也不沿用，而是落回 auto。ResolveSource 那条「不认识的一律拒绝」防
的是「明确点了 A 却下了 B」；而记忆里的「清华」是在说 Temurin 的事，对
Zulu 无从解释。显式传一个本发行版不认识的 id，仍然照旧报错。"
```

---

## Task 8: 前端

**Files:**
- Modify: `web/src/types.ts`
- Modify: `web/src/api.ts`
- Modify: `web/src/useJava.ts`
- Modify: `web/src/components/JavaPage.tsx`

**Interfaces:**
- Consumes: Task 7 的三个端点。
- Produces: 「下载设置」页上的发行版选择器与 Zulu 自定义前缀输入框。

- [ ] **Step 1: 先读 frontend-design skill**

动手前按 CLAUDE.md 的要求调用 `frontend-design` skill，照它的规则走。**本任务不新增任何 CSS**——发行版栅格复用 `choice-grid` / `choice`，自定义输入框照 `DatabasePage.tsx:773-800` 的既有形状。

- [ ] **Step 2: `types.ts`**

```ts
/** An OpenJDK build the panel can install from. */
export interface JavaDistribution {
  id: string
  name: string
  note: string
  /** The one an install gets when it names none. */
  default?: boolean
}
```

`JavaPlatform` 加 `libc?: string`；`JavaInstallJob` 加 `distribution: string`；`JavaOverview` 加：

```ts
  /** The OpenJDK builds an install can pick from, default first. */
  distributions: JavaDistribution[]
  /** The distribution the last install used; what the picker starts on. */
  distribution: string
```

- [ ] **Step 3: `api.ts`**

```ts
  javaMajors: (distribution: string) =>
    request<JavaMajor[]>(
      'GET',
      `/api/java/available?distribution=${encodeURIComponent(distribution)}`,
    ),
  installJava: (distribution: string, major: number, imageType: string, source: string) =>
    request<JavaInstallJob>('POST', '/api/java/install', {
      distribution,
      major,
      imageType,
      source,
    }),
```

- [ ] **Step 4: `useJava.ts`**

`JavaController` 加 `distributions: JavaDistribution[]`，`install` 的签名前面加 `distribution: string`。`refreshMajors` 要依赖当前发行版——两边的可装列表不一样：

```ts
  const distribution = overview?.distribution ?? ''

  const refreshMajors = useCallback(async () => {
    if (!distribution) return
    try {
      setMajors(await api.javaMajors(distribution))
    } catch {
      // The installable list comes from the distribution's own API; without it
      // the page still shows what is installed, which is the half that
      // matters offline.
      setMajors([])
    }
  }, [distribution])
```

`sources` 和 `distributions` 都从 `overview` 上取。

- [ ] **Step 5: `JavaPage.tsx`**

1. `TITLES.source` 从 `'下载源'` 改成 `'下载设置'`；`LEADS` 里两条写死 Temurin / Adoptium 的文案改成跟着选择走的函数：

```ts
function installLead(name: string): string {
  return `从 ${name} 装一个新的大版本。下载走服务器自己的网络，关掉网页也会继续。`
}

function sourceLead(name: string): string {
  return `选哪个发行版、从哪里下。装的都是 ${name} 的官方构建 —— 版本信息和校验和始终来自它自己的接口，源只负责传那几十兆的压缩包，对不上的一律不装。`
}
```

2. 组件里加状态 `const [distribution, setDistribution] = useState<string | null>(null)`，跟现有 `source` 一样从 `overview.distribution` 初始化。

3. `SourcePicker` 上面插一个发行版栅格，复用同一套类：

```tsx
<div className="choice-grid choice-grid--wide">
  {distributions.map((entry) => (
    <button
      key={entry.id}
      type="button"
      className={`choice${entry.id === distribution ? ' choice--active' : ''}`}
      aria-pressed={entry.id === distribution}
      disabled={busy}
      onClick={() => {
        setDistribution(entry.id)
        // The source lists do not overlap, so a source chosen for the other
        // distribution cannot carry over.
        setSource(null)
      }}
    >
      <span className="choice__label">
        {entry.name}
        {entry.default && <span className="badge">推荐</span>}
      </span>
      <span className="choice__note">{entry.note}</span>
    </button>
  ))}
</div>
```

4. 选中 Zulu 时，源栅格下面出自定义前缀输入框（照 `DatabasePage.tsx` 那个的结构和类名）。

5. 安装页顶上加一行指向下载设置页：

```tsx
<p className="chart-note">
  将从 <strong>{distributionName}</strong> 安装 ·{' '}
  <button type="button" className="btn btn--link" onClick={() => onOpenView('source')}>
    改
  </button>
</p>
```

6. `install` 调用补上发行版：`void java.install(distribution ?? '', major, imageType, source ?? '')`。

7. `RuntimeRow` 的标题行把 `runtime.vendor` 显出来——两个发行版混着装时必须能一眼分开：

```tsx
<span className="badge">{runtime.vendor || '未知发行方'}</span>
```

- [ ] **Step 6: 构建并人工确认**

Run: `npm --prefix web run build 2>&1 | tail -15`
Expected: `tsc -b` 与 vite 构建都通过。

人工自查（CLAUDE.md 要求，前端没有自动检查）：

- 明暗两种模式都看过；
- 1440 / 1200 / 1024 / 768 / 390 五个宽度下无横向溢出和错位；
- 折叠侧栏、打开抽屉、开着控制台的实例页这三处没被波及；
- 切换发行版时源栅格跟着换、自定义输入框只在 Zulu 下出现。

- [ ] **Step 7: 提交**

```bash
git add web/src
git commit -m "面板：Java 页能选发行版，下载源跟着走

发行版选择器放在下载设置页而不是安装页：源列表本身依赖发行版，分成两页
会让这一页的内容被一个看不见的设置改掉；而且这一页本来就是「装面板那天
或者某个镜像挂了那天」才来一次的东西，发行版正是同一类决定。

不新增 CSS，发行版栅格复用 choice-grid，自定义前缀输入框照数据库页那个
既有形状。已装列表补上发行方徽章——两个发行版混着装时得能一眼分开。"
```

---

## Task 9: CHANGELOG

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 往「未发布」里加**

**只往「未发布」里加，不要把它改成版本号标题**——那会触发自动发版。

```markdown
### 新增

- Java 运行环境可以选发行版了，**默认改为 Azul Zulu**。已装的 Temurin 运行时不受影响，继续可用；
  想装 Temurin 在「Java 环境 → 下载设置」里切回去即可。
- Alpine 等 musl 系统头一回能直接装 Java：Zulu 有 musl 构建，面板会自动选对。
- Zulu 的下载源支持自填镜像前缀，给自建或自己找到加速地址的场景用。

### 变更

- 「下载源」页改名「下载设置」，发行版和下载源在同一页选。
- 下载源列表现在跟着发行版走：清华、南大、华为、GitHub 加速都是 Adoptium 发布树的镜像，对 Zulu
  不适用，所以选中 Zulu 时不再列出。
```

- [ ] **Step 2: 跑完整检查**

Run: `make lint && make test && npm --prefix web run build 2>&1 | tail -20`
Expected: 全绿。

- [ ] **Step 3: 提交**

```bash
git add CHANGELOG.md
git commit -m "更新日志：Java 发行版可选，默认 Zulu"
```

---

## 完成后

按 `verification-before-completion` skill 拿证据，再按 `finishing-a-development-branch` skill 决定怎么合回 `main`。CLAUDE.md 的顺序是：推功能分支 → 切 `main` → `git pull origin main` → 合并 → 推 `main` → 切回功能分支。
